apiVersion: v1
kind: Service
metadata:
  name: accounts
  labels:
    app: accounts
spec:
  selector:
    app: accounts
  ports:
  - port: 8888
    targetPort: 8080
    nodePort: 30888
  type: NodePort
